meta:
  environment: jvm
  toolchain:
    name: maven
    version: 11

build:
  args: spring-boot:repackage -Pproduction

run:
  jarName: target/Password_manager-0.0.1-SNAPSHOT.jar
  containerPort: 8080

